int main()
{
    decl
        int temp;
        string FileName;
    enddecl

    begin
        temp = exposcall("Read",-1, FileName);
        temp = exposcall("Delete",FileName);
        if(temp == 0) then
            write("File Deleted");
        else
            write("File Not Deleted");
        return 0;
    end
}